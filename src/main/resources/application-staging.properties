# Staging Environment Properties
# These properties are applied ONLY when the 'staging' profile is active.
# It overrides properties in application.properties

# Example: Staging specific logging or database settings
logging.level.com.jayandhan.portfolio=DEBUG
